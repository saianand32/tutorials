package main // go is a modular language that means you can create multiple packages for your go project and split it into modules

import "fmt"

func main() {
	fmt.Println("Hello World")
}

// Note : go file names should not start with . and _ & special charactersn thats it
// - but can contain . _ and special characters in filenames except the starting character in file name.
