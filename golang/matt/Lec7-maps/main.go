package main

func main() {
	// In Go, only pointer types, slices, maps, channels, interfaces, and functions can be compared to nil.

	// cap() can be used only for slices, arrays and channels ..... cap(map) is an err cannot be used with maps
}
